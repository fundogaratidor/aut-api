name: FGC QA API Regression Tests

on:
  # Permitir disparo manual
  workflow_dispatch:

jobs:
  run-newman:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout repository
        uses: actions/checkout@v3

      - name: Install Newman and Reporter
        run: |
          npm install -g newman
          npm install -g newman-reporter-htmlextra

      - name: Create Reports Directory
        run: mkdir -p ./reports

      - name: Run Postman Collection with QA Environment
        id: run_tests
        continue-on-error: true
        run: |
          newman run qa_fgc_aws_contribuicoes.postman_collections.json \
          -e qa_fgc_aws.postman_environment.json \
          -r cli,htmlextra \
          --reporter-htmlextra-export ./reports/qa_test_report.html

      - name: Verify Report File
        run: |
          if [ ! -f ./reports/qa_test_report.html ]; then
            echo "Error: Report file not found!"
            exit 1
          fi

      - name: List Reports for Debugging
        run: |
          echo "Checking reports directory contents:"
          ls -lh ./reports || echo "Reports directory is empty or does not exist."

      - name: Send Report via Email
        uses: dawidd6/action-send-mail@v3
        with:
          server_address: smtp.gmail.com
          server_port: 465
          username: ${{ secrets.SMTP_USERNAME }}
          password: ${{ secrets.SMTP_PASSWORD }}
          subject: "QA Pipeline Test Report"
          body: "Segue em anexo o relatório dos Testes em QA gerado pela Pipeline."
          to: cristina.briguenti@fgc.org.br,silvio.aulik@gmail.com,staulik10@gmail.com
          from: "FGC QA Regression Tests API <silvio.aulik@gmail.com>"
          attachments: ./reports/qa_test_report.html

      - name: Log Failure
        if: failure()
        run: |
          echo "===================================="
          echo "🚨 Pipeline Failed!"
          echo "Branch: ${{ github.ref }}"
          echo "Commit: ${{ github.sha }}"
          echo "Verifique os logs em:"
          echo "https://github.com/${{ github.repository }}/actions/runs/${{ github.run_id }}"
          echo "===================================="


